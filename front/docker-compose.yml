version: '3'

services:
  nginx:
    image: nginx
    ports:
      - 80:80
    volumes:
      - .:/site
      - ../content:/site/content
      - ../src:/site/src
      - ./nginx.conf:/etc/nginx/conf.d/default.conf

volumes:
  db_data: